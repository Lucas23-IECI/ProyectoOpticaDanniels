# ======================
# Configuración Backend
# ======================

# Servidor
PORT=3000
HOST=localhost

# Base de datos PostgreSQL
DB_USERNAME=postgres
PASSWORD=tu_password_aqui
DATABASE=optica_daniels

# Si usas Docker, dejar DB_HOST=postgres (nombre del servicio Docker)
# Si es local, dejar vacío o DB_HOST=localhost
DB_HOST=localhost

# JWT - Token secreto para autenticación
ACCESS_TOKEN_SECRET=una_clave_secreta_larga_y_segura_aqui

# Cookie key para Passport
cookieKey=otra_clave_secreta_aqui
